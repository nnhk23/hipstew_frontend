import React from 'react'

export default class UserRecipe extends React.Component {
    render(){
        return(
            <div>
                List of recipes user has liked
                {/* render 3 columns of recipes */}
            </div>
        )
    }
}